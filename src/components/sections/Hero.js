import React from 'react';

import heroImage from '../../assets/images/header-color.jpg';

const Hero = () => (
  <div>
    <img style={{ width: '100%', height: 'auto' }} src={heroImage} alt="Keep Small Strong" />
  </div>
);

export default Hero;
